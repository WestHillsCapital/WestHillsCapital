Railway is still failing with:

`pnpm install --frozen-lockfile --prefer-offline`

and

`ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`

even after:

* adding `packageManager: pnpm@10.26.1`
* adding `nixpacks.toml`
* regenerating the lockfile

I need you to resolve this specifically for Railway/Nixpacks, not just locally.

Please do all of the following:

1. Inspect all sources of pnpm config and overrides in the repo, including:

* root `package.json`
* `pnpm-workspace.yaml`
* `.npmrc`
* any package-level `package.json`
* lockfile metadata

2. Determine whether Railway/Nixpacks is actually honoring `nixpacks.toml` and pnpm 10, or whether it is still using a default pnpm 9/stock install path.

3. If Railway may ignore the current config, make the repo compatible with Railway’s default frozen install anyway:

* move `overrides` to the correct place if needed
* remove pnpm-10-only config from `pnpm-workspace.yaml` if it breaks CI compatibility
* regenerate `pnpm-lock.yaml` from the final compatible config

4. Verify with the most Railway-like command sequence possible, not just local happy-path:

* explicitly confirm which pnpm version is being used
* run the equivalent install/build flow from repo root
* confirm frozen install succeeds

5. Check whether a Railway config file should be:

* `nixpacks.toml`
* `railway.toml`
* or neither

And confirm exactly where it must live in the repo for Railway to detect it.

6. If needed, simplify the monorepo config so Railway can build the API service reliably from repo root with:

* Root Directory blank
* no custom Railway UI config beyond environment variables

7. Commit and push the final fix to GitHub.

After that, show me:

* every file changed
* exactly what mismatch remained
* whether `overrides` ended up in `package.json`, `pnpm-workspace.yaml`, or elsewhere
* whether `minimumReleaseAge` / `onlyBuiltDependencies` were kept or removed
* whether Railway should now use `nixpacks.toml`
* the exact Railway settings I should use after your fix
* confirmation that a strict frozen install now succeeds in a Railway-compatible way
