The booking system now creates real calendar events and respects blocker calendars. The next phase is operational visibility using Google Sheets, with low tech debt and no overbuilt CRM.

Please implement Phase 2 only.

## Goal

Create a Google Sheets-based operating dashboard that mirrors the database tables and makes it easy for me to manage leads and appointments manually.

## Implement the following

### 1. Google Sheets sync

Sync data into a Google Sheet named:
MASTER APPOINTMENT/LEAD SHEET

Use 3 tabs:

* appointments
* leads
* booking_attempts

### 2. Sheet behavior

* Append new rows when new records are created
* Update matching rows when records change
* Use stable keys so updates do not create duplicates
* Keep the sheet readable and operational, not technical

### 3. appointments tab

Mirror the appointments table with useful operating columns, including:

* confirmation_id
* slot_id
* scheduled_time
* day_label
* time_label
* first_name
* last_name
* email
* phone
* state
* allocation_type
* allocation_range
* timeline
* status
* lead linkage field
* calendar_event_id
* created_at
* updated_at
* notes

### 4. leads tab

Mirror the leads table with useful operating columns, including:

* lead identifier
* first_name
* last_name
* email
* phone
* state
* allocation_type
* allocation_range
* timeline
* source
* status
* current_custodian
* form_type
* linked appointment confirmation_id
* created_at
* updated_at
* notes
* follow_up_date
* owner

### 5. booking_attempts tab

Mirror booking_attempts as an audit log with:

* attempt identifier
* contact info
* requested slot
* result
* error_message
* confirmation_id if successful
* created_at

### 6. Status model

Add support for these statuses if not already present:

Appointments:

* confirmed
* completed
* no_show
* cancelled
* rescheduled

Leads:

* new
* contacted
* qualified
* won
* lost
* nurture

### 7. Lead and appointment linkage

* Ensure booked appointments can be linked back to a lead record
* Expose that linkage in both the database and the Google Sheet
* Keep the linkage simple and reliable

## Scope constraints

* Do NOT build a custom CRM UI yet
* Do NOT build invoicing or shipping automation yet
* Do NOT add referral or birthday workflows yet
* Do NOT make Google Sheets the system of truth
* PostgreSQL remains the source of truth; Google Sheets is the operational mirror

## After implementing, tell me:

* which files changed
* what Google credentials or setup I need to provide
* how row matching and updates work
* how duplicate rows are prevented
* how appointment and lead linkage works
* what statuses are now supported
